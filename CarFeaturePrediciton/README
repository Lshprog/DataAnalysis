Run the entire script

After script executed, the results will be saved into submission_ensemble_best.csv

Libraries used:
- glmnet
- caret
- nnet
- xgboost


if (!require(glmnet)) {
  install.packages("glmnet")
  library(glmnet)
}

if (!require(caret)) {
  install.packages("caret")
  library(caret)
}

if (!require(nnet)) {
  install.packages("nnet")
  library(nnet)
}

# Load necessary library
if (!require(xgboost)) {
  install.packages("xgboost")
  library(xgboost)
}